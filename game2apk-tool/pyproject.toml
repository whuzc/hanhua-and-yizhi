[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "game2apk-tool"
version = "0.1.0"
description = "Safe RPG Maker MV Windows staging and Android packaging services"
requires-python = ">=3.11,<3.12"
dependencies = []
